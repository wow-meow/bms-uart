# Build outputs (objects in build/; binary in install/bin/)
build/
install/
bin/

# Runtime CSV logs written by the tool
logs/

# Keep only the protocol PDF under docs/; ignore everything else there
docs/*
!docs/485UART通用协议V4.pdf

# Stale root binary from old in-tree build layout
bms_query

# Editor / tooling artifacts
compile_commands.json
.clangd
.cache/

# npm
package.json
pnpm-lock.yaml
node_modules/

